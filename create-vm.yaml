# azure-pipelines/create-vm.yaml
steps:
  - script: bash scripts/az-login.sh
    displayName: "Login to Azure"

  - script: bash scripts/create-vm.sh
    displayName: "Create VM using Ansible"
    workingDirectory: $(Build.SourcesDirectory)